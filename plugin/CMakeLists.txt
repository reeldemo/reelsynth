# Retired — JUCE scaffold removed.
# ReelSynth UI is egui-only. See plugin/README.md.

cmake_minimum_required(VERSION 3.22)
project(reelsynth-plugin-stub)
message(STATUS "Use cargo build for ReelSynth (egui + Rust plugin host). JUCE not used.")
